#include "vehicle_bridge/vehicle_state.h"

// VehicleState is a plain data struct; all logic lives in the header.
// This translation unit exists so the library target always has at least
// one object file per header, making future additions straightforward.
